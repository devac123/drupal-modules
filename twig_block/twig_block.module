<?php

function twig_block_theme(){
    return array(
       "custom" => array(
           'variables' =>  array
            (
               "params" => [],
               "title" =>   "this is title comming from sample_api dot module file",
            )
        )
    );
}